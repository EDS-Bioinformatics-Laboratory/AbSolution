# 4_Clonal_exploration

A utils function

## Usage

``` r
calculate_clone(
  seq_df,
  clonotype,
  AA_or_NT = "NT",
  region = "CDR3",
  percentage = 100,
  calculate_shared_clones
)
```

## Value

The return value, if any, from executing the utility.
